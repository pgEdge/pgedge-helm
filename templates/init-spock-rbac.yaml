{{- if (coalesce .Values.pgEdge.initSpock .Values.pgEdge.initSpockJob.enabled) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.pgEdge.appName }}-init-spock
  {{- if $.Values.annotations }}
  annotations:
    {{- $.Values.annotations | toYaml | nindent 4 }}
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Values.pgEdge.appName }}-init-spock
rules:
  - apiGroups: ["postgresql.cnpg.io"]
    resources: ["clusters"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.pgEdge.appName }}-init-spock
subjects:
  - kind: ServiceAccount
    name: {{ .Values.pgEdge.appName }}-init-spock
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: {{ .Values.pgEdge.appName }}-init-spock
{{- end }}